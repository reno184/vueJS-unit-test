/* describe('Ajax provider', function () {
  it('should have list method', function () {
    const ajaxProviderMock = {
      list: jest.fn().mockReturnValue(4),
      find: jest.fn().mockReturnValue(4),
      posts: jest.fn().mockReturnValue(4),
      put: jest.fn().mockReturnValue(4),
      delete: jest.fn().mockReturnValue(4)
    }
    const CreateAjaxProviderMock = jest.fn().mockReturnValue(ajaxProviderMock)
    expect(CreateAjaxProviderMock().list).toHaveBeenCalledWith('pong')
  })
}) */
